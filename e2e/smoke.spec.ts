import { expect,test } from "@playwright/test";
test("demo dashboard and shipment workflow are accessible",async({page})=>{await page.goto("/dashboard");await expect(page.getByRole("heading",{name:/Good morning/})).toBeVisible();await page.getByRole("link",{name:"Shipments"}).click();await expect(page.getByRole("heading",{name:"Shipments"})).toBeVisible();await page.getByPlaceholder(/Search reference/).fill("FR-003");await expect(page.getByText("Warsaw")).toBeVisible()});
