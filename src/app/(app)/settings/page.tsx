import {PageHeader} from "@/components/page-header";import {SettingsForm} from "@/components/settings-form";import {getProfile} from "@/lib/data/reporting";
export default async function SettingsPage(){const profile=await getProfile();return <><PageHeader title="Settings" description="Manage your workspace identity and reporting currency."/>{profile.isDemo?<div className="max-w-3xl rounded-xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-800">Settings are read-only in the public portfolio demo.</div>:<SettingsForm fullName={profile.fullName} email={profile.email} currency={profile.reportingCurrency}/>}</>}
