import Link from "next/link";
import { ArrowUpRight, CircleDollarSign, PackageCheck, Percent, Plus, Route, WalletCards } from "lucide-react";
import { PageHeader } from "@/components/page-header"; import { Button } from "@/components/ui/button"; import { Card, CardContent, CardHeader } from "@/components/ui/card"; import { KpiCard } from "@/components/dashboard/kpi-card"; import { PerformanceChart } from "@/components/analytics/performance-chart"; import { StatusBadge } from "@/components/ui/badge"; import { shipments } from "@/data/mock-data"; import { formatMoney } from "@/lib/utils";
export default function DashboardPage(){return <><PageHeader title="Good morning, Alex" description="Here’s what is happening with your freight operations today."><Link href="/shipments/new"><Button><Plus size={17}/>Add shipment</Button></Link></PageHeader><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><KpiCard label="Active Shipments" value="7" detail="+2 since yesterday" icon={Route}/><KpiCard label="Completed This Month" value="24" detail="+14% from last month" icon={PackageCheck} tone="blue"/><KpiCard label="Monthly Revenue" value="PLN 48,200" detail="+7.4% from last month" icon={CircleDollarSign}/><KpiCard label="Monthly Costs" value="PLN 36,700" detail="+5.1% from last month" icon={WalletCards} tone="amber"/><KpiCard label="Monthly Profit" value="PLN 11,500" detail="+15.8% from last month" icon={ArrowUpRight}/><KpiCard label="Average Margin" value="23.8%" detail="+1.2 pts from last month" icon={Percent} tone="blue"/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.65fr_1fr]"><Card><CardHeader><div><h2 className="font-semibold text-slate-900">Financial performance</h2><p className="mt-1 text-xs text-slate-500">Revenue, costs and profit over 6 months</p></div><div className="flex gap-3 text-xs text-slate-500"><span>● Revenue</span><span className="text-slate-400">● Costs</span><span className="text-amber-500">● Profit</span></div></CardHeader><CardContent><PerformanceChart/></CardContent></Card><Card><CardHeader><div><h2 className="font-semibold">Recent shipments</h2><p className="mt-1 text-xs text-slate-500">Latest operational updates</p></div><Link href="/shipments" className="text-xs font-semibold text-emerald-700">View all</Link></CardHeader><CardContent className="space-y-1">{shipments.slice(0,4).map(s=><Link href={`/shipments/${s.id}`} key={s.id} className="flex items-center justify-between border-b border-slate-100 py-3 last:border-0"><div><p className="text-sm font-semibold">{s.referenceNumber}</p><p className="mt-1 text-xs text-slate-500">{s.pickupCity} → {s.deliveryCity}</p></div><div className="text-right"><StatusBadge status={s.status}/><p className="mt-1.5 text-xs font-medium">{formatMoney(s.profit,s.currency)}</p></div></Link>)}</CardContent></Card></div></>}
