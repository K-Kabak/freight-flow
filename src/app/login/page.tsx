import Link from "next/link"; import { AuthForm } from "@/components/auth-form"; import { AuthShell } from "@/components/auth-shell"; import { isSupabaseConfigured } from "@/lib/supabase/config";
export default function Login(){const configured=isSupabaseConfigured();return <AuthShell title="Welcome back" description="Sign in to manage your freight operations." footer={<>New to FreightFlow? <Link className="font-semibold text-emerald-700" href="/register">Create account</Link></>}><AuthForm mode="login"/>{!configured&&<><div className="mt-5 flex items-center gap-3 text-xs text-slate-400"><i className="h-px flex-1 bg-slate-200"/>PORTFOLIO DEMO<i className="h-px flex-1 bg-slate-200"/></div><Link href="/dashboard" className="mt-5 flex h-10 items-center justify-center rounded-lg border text-sm font-semibold hover:bg-slate-50">Explore read-only demo</Link></>}</AuthShell>}
