"use client";import {Bar,BarChart,CartesianGrid,Cell,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from "recharts";import type {Currency,ShipmentStatus} from "@/types";
const colors=["#2563eb","#7c3aed","#f59e0b","#178563","#64748b","#dc2626"];
export function StatusChart({data}:{data:{name:ShipmentStatus;value:number}[]}){return <div className="h-64"><ResponsiveContainer><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={55} outerRadius={86} paddingAngle={3}>{data.map((item,index)=><Cell fill={colors[index]} key={item.name}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><div className="-mt-2 flex flex-wrap justify-center gap-3">{data.map((item,index)=><span key={item.name} className="text-xs text-slate-500"><i className="mr-1 inline-block size-2 rounded-full" style={{background:colors[index]}}/>{item.name}</span>)}</div></div>}
export function ProfitChart({data,currency}:{data:{name:string;profit:number}[];currency:Currency}){return <div className="h-72"><ResponsiveContainer><BarChart data={data} margin={{left:-15}}><CartesianGrid vertical={false} stroke="#eef0f3"/><XAxis dataKey="name" axisLine={false} tickLine={false}/><YAxis axisLine={false} tickLine={false} tickFormatter={value=>`${value/1000}k`}/><Tooltip formatter={value=>`${currency} ${Number(value).toLocaleString()}`}/><Bar dataKey="profit" fill="#176b57" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div>}
