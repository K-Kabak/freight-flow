"use client";
import { useState } from "react"; import { Search, Star } from "lucide-react"; import { clients, carriers } from "@/data/mock-data"; import { Input } from "@/components/ui/input"; import { formatMoney } from "@/lib/utils";
export function DirectoryTable({type}:{type:"clients"|"carriers"}){const[q,setQ]=useState("");const data=(type==="clients"?clients:carriers).filter(x=>x.companyName.toLowerCase().includes(q.toLowerCase()));return <div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="relative border-b p-4"><Search className="absolute left-7 top-7 text-slate-400" size={16}/><Input value={q} onChange={e=>setQ(e.target.value)} className="max-w-md pl-9" placeholder={`Search ${type}…`}/></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{(type==="clients"?["Company","Tax ID","Contact","Total shipments","Revenue","Avg. margin"]:["Company","Country","Contact","Vehicle type","Rating","Completed"]).map(x=><th className="px-5 py-3" key={x}>{x}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{type==="clients"?clients.filter(x=>x.companyName.toLowerCase().includes(q.toLowerCase())).map(c=><tr className="hover:bg-slate-50" key={c.id}><td className="px-5 py-4 font-semibold">{c.companyName}</td><td className="px-5 py-4 text-slate-500">{c.taxId}</td><td className="px-5 py-4"><p>{c.contactPerson}</p><p className="text-xs text-slate-500">{c.email}</p></td><td className="px-5 py-4">{c.totalShipments}</td><td className="px-5 py-4 font-medium">{formatMoney(c.totalRevenue)}</td><td className="px-5 py-4 text-emerald-700">{c.averageMargin}%</td></tr>):carriers.filter(x=>x.companyName.toLowerCase().includes(q.toLowerCase())).map(c=><tr className="hover:bg-slate-50" key={c.id}><td className="px-5 py-4 font-semibold">{c.companyName}</td><td className="px-5 py-4">{c.country}</td><td className="px-5 py-4"><p>{c.contactPerson}</p><p className="text-xs text-slate-500">{c.email}</p></td><td className="px-5 py-4">{c.vehicleType}</td><td className="px-5 py-4"><span className="flex items-center gap-1 font-medium"><Star size={15} className="fill-amber-400 text-amber-400"/>{c.rating}.0</span></td><td className="px-5 py-4">{c.completedShipments}</td></tr>)}</tbody></table></div></div>}
