"use client";
import { useMemo, useState } from "react"; import Link from "next/link"; import { ChevronLeft, ChevronRight, Search, SlidersHorizontal } from "lucide-react"; import { shipments } from "@/data/mock-data"; import { formatDate, formatMoney } from "@/lib/utils"; import { Input, Select } from "@/components/ui/input"; import { StatusBadge } from "@/components/ui/badge"; import { shipmentStatuses } from "@/types";
export function ShipmentsTable(){const[q,setQ]=useState("");const[status,setStatus]=useState("All");const rows=useMemo(()=>shipments.filter(s=>(status==="All"||s.status===status)&&`${s.referenceNumber} ${s.client} ${s.pickupCity} ${s.deliveryCity}`.toLowerCase().includes(q.toLowerCase())),[q,status]);return <div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-200 p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-slate-400" size={16}/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search reference, client or route…" className="pl-9"/></div><div className="relative sm:w-48"><SlidersHorizontal className="pointer-events-none absolute left-3 top-3 text-slate-400" size={16}/><Select value={status} onChange={e=>setStatus(e.target.value)} className="pl-9"><option>All</option>{shipmentStatuses.map(s=><option key={s}>{s}</option>)}</Select></div></div><div className="overflow-x-auto"><table className="w-full min-w-[1080px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{["Reference","Route","Client / Carrier","Dates","Revenue","Profit","Margin","Status"].map(h=><th key={h} className="px-5 py-3 font-semibold">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map(s=><tr key={s.id} className="hover:bg-slate-50/70"><td className="px-5 py-4"><Link className="font-semibold text-emerald-800 hover:underline" href={`/shipments/${s.id}`}>{s.referenceNumber}</Link></td><td className="px-5 py-4 font-medium">{s.pickupCity} <span className="text-slate-400">→</span> {s.deliveryCity}</td><td className="px-5 py-4"><p>{s.client}</p><p className="mt-1 text-xs text-slate-500">{s.carrier}</p></td><td className="px-5 py-4"><p>{formatDate(s.pickupDate)}</p><p className="mt-1 text-xs text-slate-500">to {formatDate(s.deliveryDate)}</p></td><td className="px-5 py-4 font-medium">{formatMoney(s.clientPrice,s.currency)}</td><td className="px-5 py-4 font-semibold text-emerald-700">{formatMoney(s.profit,s.currency)}</td><td className="px-5 py-4">{s.marginPercent}%</td><td className="px-5 py-4"><StatusBadge status={s.status}/></td></tr>)}</tbody></table>{!rows.length&&<div className="p-12 text-center text-sm text-slate-500">No shipments match your filters.</div>}</div><div className="flex items-center justify-between border-t border-slate-200 px-5 py-4 text-sm text-slate-500"><span>Showing {rows.length} of {shipments.length} shipments</span><div className="flex gap-1"><button className="rounded-md border p-1.5"><ChevronLeft size={16}/></button><button className="rounded-md border p-1.5"><ChevronRight size={16}/></button></div></div></div>}
