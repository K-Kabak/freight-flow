"use client";
import { useRouter } from "next/navigation"; import { toast } from "sonner"; import { Button } from "@/components/ui/button"; import { Card, CardContent } from "@/components/ui/card"; import { Input, Label, Select } from "@/components/ui/input";
export function EntityForm({type}:{type:"client"|"carrier"}){const router=useRouter();const fields=type==="client"?[["Company name","text"],["Tax / VAT ID","text"],["Contact person","text"],["Email","email"],["Phone","tel"]]:[["Company name","text"],["Country","text"],["Contact person","text"],["Email","email"],["Phone","tel"],["Vehicle type","text"]];return <form onSubmit={e=>{e.preventDefault();toast.success(`${type==="client"?"Client":"Carrier"} created`);router.push(type==="client"?"/clients":"/carriers")}}><Card className="max-w-3xl"><CardContent className="grid gap-5 sm:grid-cols-2">{fields.map(([label,inputType])=><div key={label}><Label>{label}</Label><Input required type={inputType} placeholder={label}/></div>)}{type==="carrier"&&<div><Label>Rating</Label><Select defaultValue="5">{[1,2,3,4,5].map(v=><option value={v} key={v}>{v} stars</option>)}</Select></div>}<div className="flex gap-2 sm:col-span-2"><Button type="button" variant="outline" onClick={()=>router.back()}>Cancel</Button><Button type="submit">Create {type}</Button></div></CardContent></Card></form>}
