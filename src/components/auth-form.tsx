"use client";
import Link from "next/link"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { toast } from "sonner"; import { createClient } from "@/lib/supabase/client"; import { Button } from "@/components/ui/button"; import { Input, Label } from "@/components/ui/input";
export function AuthForm({mode}:{mode:"login"|"register"|"reset"}){const router=useRouter();const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const form=new FormData(e.currentTarget);const email=String(form.get("email"));const password=String(form.get("password")||"");const supabase=createClient();if(!supabase){toast.info("Demo mode enabled",{description:"Connect Supabase to use live authentication."});router.push("/dashboard");return}let error;if(mode==="login")({error}=await supabase.auth.signInWithPassword({email,password}));else if(mode==="register")({error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:`${location.origin}/auth/callback`}}));else({error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/auth/callback?next=/settings`}));setLoading(false);if(error)return toast.error(error.message);toast.success(mode==="login"?"Welcome back":"Check your inbox");if(mode==="login")router.push("/dashboard")}
return <form onSubmit={submit} className="space-y-4"><div><Label>Email address</Label><Input name="email" type="email" required placeholder="you@company.com"/></div>{mode!=="reset"&&<div><div className="flex justify-between"><Label>Password</Label>{mode==="login"&&<Link href="/forgot-password" className="text-xs font-semibold text-emerald-700">Forgot password?</Link>}</div><Input name="password" type="password" required minLength={8} placeholder="At least 8 characters"/></div>}<Button disabled={loading} className="w-full">{loading?"Please wait…":mode==="login"?"Sign in":mode==="register"?"Create account":"Send reset link"}</Button></form>}
