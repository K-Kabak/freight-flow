import { describe,expect,it } from "vitest"; import { calculateMarginPercent, calculateProfit, convertToReportingCurrency } from "./calculations";
describe("freight calculations",()=>{it("calculates profit after all costs",()=>expect(calculateProfit(4200,3300,150)).toBe(750));it("calculates and rounds margin",()=>expect(calculateMarginPercent(750,4200)).toBe(17.86));it("returns zero margin for zero revenue",()=>expect(calculateMarginPercent(-100,0)).toBe(0));it("preserves a loss",()=>expect(calculateProfit(100,120,10)).toBe(-30));it("converts using the stored FX snapshot",()=>expect(convertToReportingCurrency(100,4.28)).toBe(428))});
